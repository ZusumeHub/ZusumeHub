local service = 3432
local secret = "55acd596-9e84-4dbd-934e-9def6297d4d3"
local useNonce = true
local onMessage = function(message) game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", { Text = message }) end
local mainScript = "https://raw.githubusercontent.com/ZusumeHub/ZusumeHub/refs/heads/main/opws"

repeat task.wait() until game:IsLoaded() and game.Players.LocalPlayer

-- Sistem Key Manager
local function saveKey(key)
    if writefile then
        writefile("platoboost_key.txt", key)
    else
        (syn and syn.queue_on_teleport or queue_on_teleport)('_G.savedKey = "'..key..'"')
    end
end

local function loadSavedKey()
    local key = ""
    if readfile and pcall(readfile, "platoboost_key.txt") then
        key = readfile("platoboost_key.txt")
    end
    return _G.savedKey or key
end

-- Sistem Verifikasi
local fRequest = request or (syn and syn.request) or http_request
local HttpService = game:GetService("HttpService")
local cachedLink, cachedTime = "", 0

local function lDigest(input)
    local hash = {}
    for i = 1, #input do
        hash[i] = string.byte(input, i)
    end
    return table.concat(hash, ""):gsub(".", function(c) return string.format("%02x", c:byte()) end)
end

local function verifyKey(key)
    local response = fRequest({
        Url = "https://api.platoboost.com/public/whitelist/"..service.."?identifier="..lDigest(tostring(game:GetService("Players").LocalPlayer.UserId)).."&key="..key,
        Method = "GET"
    })
    
    if response.StatusCode == 200 then
        local data = HttpService:JSONDecode(response.Body)
        if data.success and data.data.valid then
            saveKey(key)
            return true
        end
    end
    return false
end

-- Auto-Verify Key yang Tersimpan
local savedKey = loadSavedKey()
if savedKey and verifyKey(savedKey) then
    loadstring(game:HttpGet(mainScript))()
    return
end

-- Modern GUI Design
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local gui = Instance.new("ScreenGui")
if syn and syn.protect_gui then
    syn.protect_gui(gui)
    gui.Parent = game:GetService("CoreGui")
else
    gui.Parent = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
end

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 300, 0, 180)
mainFrame.Position = UDim2.new(0.5, -150, 0.5, -90)
mainFrame.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
mainFrame.BorderSizePixel = 0
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Parent = gui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 8)
uiCorner.Parent = mainFrame

local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 32)
topBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
topBar.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Text = "KEY SYSTEM"
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.TextColor3 = Color3.fromRGB(200, 200, 200)
title.Size = UDim2.new(0.6, 0, 1, 0)
title.Position = UDim2.new(0.02, 0, 0, 0)
title.BackgroundTransparency = 1
title.Parent = topBar

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 32, 0, 32)
closeBtn.Position = UDim2.new(1, -32, 0, 0)
closeBtn.Text = "×"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 24
closeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
closeBtn.BackgroundTransparency = 1
closeBtn.Parent = topBar

local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Size = UDim2.new(0, 32, 0, 32)
minimizeBtn.Position = UDim2.new(1, -64, 0, 0)
minimizeBtn.Text = "-"
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 24
minimizeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
minimizeBtn.BackgroundTransparency = 1
minimizeBtn.Parent = topBar

local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -20, 1, -50)
contentFrame.Position = UDim2.new(0.5, -10, 0, 40)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

local keyBox = Instance.new("TextBox")
keyBox.Size = UDim2.new(1, 0, 0, 36)
keyBox.PlaceholderText = "Enter your key here..."
keyBox.Font = Enum.Font.Gotham
keyBox.TextSize = 12
keyBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
keyBox.TextColor3 = Color3.fromRGB(200, 200, 200)
keyBox.Parent = contentFrame

local getKeyBtn = Instance.new("TextButton")
getKeyBtn.Size = UDim2.new(1, 0, 0, 32)
getKeyBtn.Position = UDim2.new(0, 0, 0, 44)
getKeyBtn.Text = "GET KEY"
getKeyBtn.Font = Enum.Font.GothamBold
getKeyBtn.TextSize = 12
getKeyBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 215)
getKeyBtn.TextColor3 = Color3.new(1, 1, 1)
getKeyBtn.Parent = contentFrame

local checkKeyBtn = Instance.new("TextButton")
checkKeyBtn.Size = UDim2.new(1, 0, 0, 32)
checkKeyBtn.Position = UDim2.new(0, 0, 0, 82)
checkKeyBtn.Text = "CHECK KEY"
checkKeyBtn.Font = Enum.Font.GothamBold
checkKeyBtn.TextSize = 12
checkKeyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
checkKeyBtn.TextColor3 = Color3.new(1, 1, 1)
checkKeyBtn.Parent = contentFrame

-- Animasi dan Interaksi
local function createRippleEffect(button)
    local ripple = Instance.new("Frame")
    ripple.Size = UDim2.new(0, 0, 0, 0)
    ripple.BackgroundColor3 = Color3.new(1, 1, 1)
    ripple.BackgroundTransparency = 0.8
    ripple.Parent = button
    
    local mousePos = UserInputService:GetMouseLocation()
    local buttonPos = button.AbsolutePosition
    local pos = UDim2.new(0, mousePos.X - buttonPos.X, 0, mousePos.Y - buttonPos.Y)
    
    ripple:TweenSizeAndPosition(
        UDim2.new(1, 0, 1, 0),
        pos + UDim2.new(-0.5, 0, -0.5, 0),
        "Out", "Quad", 0.3
    )
    
    spawn(function()
        wait(0.3)
        ripple:Destroy()
    end)
end

-- Sistem Drag
local dragging = false
local dragInput, dragStart, startPos

local function updateInput(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

topBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

topBar.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateInput(input)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

-- Fungsi Tombol
closeBtn.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

getKeyBtn.MouseButton1Click:Connect(function()
    setclipboard("https://platoboost.com/key-system")
    onMessage("Link copied to clipboard!")
end)

checkKeyBtn.MouseButton1Click:Connect(function()
    if keyBox.Text ~= "" then
        if verifyKey(keyBox.Text) then
            loadstring(game:HttpGet(mainScript))()
            gui:Destroy()
        else
            onMessage("Invalid key!")
        end
    end
end)

-- Efek Hover
local function buttonHoverEffect(button)
    button.MouseEnter:Connect(function()
        TweenService:Create(button, TweenInfo.new(0.2), {
            BackgroundColor3 = button == getKeyBtn 
                and Color3.fromRGB(0, 97, 176) 
                or Color3.fromRGB(60, 60, 60)
        }):Play()
    end)
    
    button.MouseLeave:Connect(function()
        TweenService:Create(button, TweenInfo.new(0.2), {
            BackgroundColor3 = button == getKeyBtn 
                and Color3.fromRGB(0, 120, 215) 
                or Color3.fromRGB(40, 40, 40)
        }):Play()
    end)
end

buttonHoverEffect(getKeyBtn)
buttonHoverEffect(checkKeyBtn)